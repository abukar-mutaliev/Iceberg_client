# 🚀 Быстрый старт: Публикация в Play Market

## ✅ Что было настроено:

1. **Production профиль** в `eas.json` настроен для сборки AAB (Android App Bundle)
2. **Development Client отключен** для production сборок
3. **Добавлены скрипты** для сборки и публикации обновлений
4. **OTA обновления** уже настроены и готовы к работе

## ⚠️ ПЕРЕД ПУБЛИКАЦИЕЙ: Регистрация аккаунта разработчика

**Важно:** Для публикации в Google Play нужен аккаунт разработчика.

### Для организации требуется:
1. **Номер DUNS** - получить на https://www.dnb.com/duns-number.html (бесплатно, 1-5 дней)
2. **Документы организации** - учредительные документы, свидетельство о регистрации
3. **Полномочия** - право действовать от имени организации
4. **Регистрационный взнос** - $25 USD (единоразово)

**Подробная инструкция:** см. `GOOGLE_PLAY_SETUP.md`

### 💡 О платежах в приложении:

Ваше приложение использует **внешнюю платежную систему** (Т-Банк/ЮKassa) для оплаты товаров.

- ✅ Это разрешено для **физических товаров/услуг**
- ⚠️ Разрешение `com.android.vending.BILLING` в конфигурации нужно только если планируете использовать Google Play Billing (премиум подписки, цифровой контент)
- 📝 Если не планируете - можно убрать это разрешение (см. `GOOGLE_PLAY_SETUP.md`)

## 📦 Первая публикация в Play Market:

### 1. Соберите production версию:
```bash
cd mobile
npm run build:production
```

### 2. Дождитесь завершения сборки (15-30 минут)
После завершения вы получите ссылку на скачивание AAB файла.

### 3. Загрузите в Google Play Console:
- Войдите на https://play.google.com/console
- Создайте приложение (если еще не создано)
- Перейдите в "Производство" → "Создать релиз"
- Загрузите AAB файл
- Заполните описание и отправьте на проверку

## 🔄 Публикация обновлений (без пересборки):

После того как приложение опубликовано, вы можете обновлять код без пересборки:

```bash
# Если изменения только в JavaScript коде:
npm run update:production
```

**Обновления будут автоматически применены у всех пользователей!**

## ⚠️ Когда нужна пересборка:

Пересборка нужна только если вы:
- Добавили/удалили нативные модули
- Изменили `app.config.js` (permissions, plugins и т.д.)
- Изменили `versionCode` (для новой версии в Play Market)

В этом случае:
1. Обновите `versionCode` в `app.config.js`
2. Выполните `npm run build:production`
3. Загрузите новый AAB в Play Console

## 📝 Важные файлы:

- `eas.json` - конфигурация сборок
- `app.config.js` - конфигурация приложения
- `package.json` - скрипты для сборки и обновлений

## 📖 Подробная документация:

См. `DEPLOYMENT.md` для полной инструкции.

