# 📱 Интеграция системы логистики в мобильное приложение

## 🎯 Обзор изменений

Мобильное приложение полностью интегрировано с новой системой логистических ограничений и альтернативных предложений для обеспечения лучшего клиентского опыта.

## ✅ Реализованные компоненты

### 🔄 **1. Обновленные статусы заказов**

#### Новый статус: `WAITING_STOCK`
```javascript
// Обновлено в mobile/src/entities/order/lib/utils.js
export const ORDER_STATUSES = {
    PENDING: 'PENDING',
    CONFIRMED: 'CONFIRMED',
    WAITING_STOCK: 'WAITING_STOCK', // ← НОВЫЙ
    IN_DELIVERY: 'IN_DELIVERY',
    DELIVERED: 'DELIVERED',
    CANCELLED: 'CANCELLED',
    RETURNED: 'RETURNED'
};
```

#### Обновленная логика отмены:
```javascript
// Клиенты могут отменять заказы в статусе WAITING_STOCK
export const canCancelOrder = (status, userRole = 'CLIENT') => {
    if (userRole === 'CLIENT') {
        return [ORDER_STATUSES.PENDING, ORDER_STATUSES.WAITING_STOCK].includes(status);
    }
    // ...
};
```

### 💭 **2. API для альтернативных предложений**

#### Новый API модуль: `OrderAlternativesApi`
```javascript

export class OrderAlternativesApi {
    // Получить активные предложения выбора
    static async getMyChoices()
    
    // Ответить на предложение
    static async respondToChoice(choiceId, response, selectedAlternativeId, responseData)
    
    // Получить детали предложения
    static async getChoiceDetails(choiceId)
    
    // Найти товары-заменители
    static async getProductSubstitutes(productId, districtId)
}
```

#### Константы и типы:
```javascript
export const CHOICE_TYPES = {
    STOCK_UNAVAILABLE: 'STOCK_UNAVAILABLE',     // Товар недоступен
    DELIVERY_DISTANCE: 'DELIVERY_DISTANCE',     // Склад далеко
    PARTIAL_AVAILABILITY: 'PARTIAL_AVAILABILITY', // Частично доступен
    PRODUCT_SUBSTITUTE: 'PRODUCT_SUBSTITUTE'    // Предложение замены
};

export const ALTERNATIVE_TYPES = {
    WAIT_STOCK: 'WAIT_STOCK',           // Подождать
    SUBSTITUTE: 'SUBSTITUTE',           // Заменить
    PARTIAL_ORDER: 'PARTIAL_ORDER',     // Частичный заказ
    CANCEL_ORDER: 'CANCEL_ORDER',       // Отменить
    DISTANT_DELIVERY: 'DISTANT_DELIVERY' // Дальняя доставка
};
```

### 📱 **3. Экран выбора альтернатив**

#### `OrderChoiceScreen`
- **Путь**: `mobile/src/screens/orderAlternatives/ui/OrderChoiceScreen/ui/OrderChoiceScreen.jsx`
- **Функциональность**:
  - Отображение проблемы с заказом
  - Список альтернативных решений
  - Таймер до истечения времени выбора
  - Подтверждение/отклонение выбора
  - Обработка истекших предложений

#### Особенности UI:
```javascript
// Адаптивные цвета для типов альтернатив
export const ALTERNATIVE_TYPE_COLORS = {
    WAIT_STOCK: '#fd7e14',      // Оранжевый - ожидание
    SUBSTITUTE: '#007bff',      // Синий - замена
    PARTIAL_ORDER: '#ffc107',   // Желтый - частично
    CANCEL_ORDER: '#dc3545',    // Красный - отмена
    DISTANT_DELIVERY: '#17a2b8' // Голубой - дальняя доставка
};
```

### 🔔 **4. Система уведомлений**

#### `ChoiceNotificationBanner` 
- Баннер в `MyOrdersScreen` для привлечения внимания
- Индикация срочных предложений (< 2 часов)
- Счетчик активных предложений

#### `WaitingStockIndicator`
- Индикатор статуса ожидания в деталях заказа
- Прогресс-бар ожидания (до 7 дней)
- Информация об ожидаемой дате поступления

#### Push-уведомления:
```javascript
// mobile/src/services/orderChoiceNotifications.service.js
class OrderChoiceNotificationsService {
    // Обработка уведомлений о предложениях
    static handleChoiceNotification(notificationData)
    
    // Планирование напоминаний
    static scheduleUrgentReminder(choiceId, orderId, expiresAt)
    
    // Обновление badge приложения
    static async updateChoicesBadge()
}
```

### 💳 **5. Предавторизация платежей**

#### Обновленный checkout процесс:
```javascript
// В CheckoutScreen добавлена предавторизация по умолчанию
const result = await CartService.checkout({
    // ... другие поля
    usePreauthorization: true // ← Новое поле
});

// Обработка случая требования выбора клиента
if (order?.requiresClientChoice) {
    // Переход к экрану выбора альтернатив
    navigation.navigate('OrderChoice', {
        choiceId: order.clientChoiceId,
        orderId: order.id
    });
}
```

#### Информационный экран: `PreauthorizationInfoScreen`
- Объяснение процесса предавторизации
- Преимущества для клиента
- Возможные сценарии развития событий

## 🚀 Пользовательские сценарии

### **Сценарий 1: Обычный заказ**
1. Клиент оформляет заказ → средства блокируются
2. Товар есть на ближайшем складе → заказ обрабатывается
3. При отправке → средства списываются

### **Сценарий 2: Товар недоступен**
1. Клиент оформляет заказ → средства блокируются  
2. Товара нет на складе → создается предложение выбора
3. Клиент получает push-уведомление
4. Клиент видит баннер в MyOrdersScreen
5. Клиент переходит в OrderChoiceScreen
6. Выбирает вариант: ждать/заменить/отменить
7. Система выполняет выбранное действие

### **Сценарий 3: Склад далеко**
1. Товар есть только на дальнем складе (>30км)
2. Система предлагает варианты:
   - Подождать поступления на ближний склад
   - Доставку с дальнего склада (+300₽, предупреждение)
   - Отмену заказа
3. Клиент выбирает через удобный UI

## 🎨 UI/UX улучшения

### **Визуальные индикаторы:**
- 🟠 **WAITING_STOCK** - оранжевый цвет, иконка "inventory"
- ⚡ **Срочные предложения** - пульсирующий оранжевый баннер
- 📊 **Прогресс ожидания** - прогресс-бар до 7 дней
- 🕐 **Таймер выбора** - обратный отсчет времени

### **Адаптивная навигация:**
```javascript
// Автоматический переход к выбору после checkout
if (order?.requiresClientChoice) {
    navigation.reset({
        index: 0,
        routes: [
            { name: 'MainTab', params: { screen: 'Main' } },
            { name: 'OrderChoice', params: { choiceId, orderId } }
        ]
    });
}
```

### **Умные уведомления:**
- 🔔 **Мгновенные** - при создании предложения
- ⏰ **Напоминания** - за 30 минут до истечения
- 📱 **Badge счетчик** - количество активных предложений

## 🔗 Интеграция с навигацией

### Новые экраны в навигации:
```javascript
// Добавить в навигатор приложения:
<Stack.Screen 
    name="OrderChoice" 
    component={OrderChoiceScreen}
    options={{ 
        title: 'Выберите вариант',
        headerStyle: { backgroundColor: '#667eea' },
        headerTintColor: '#fff'
    }}
/>

<Stack.Screen 
    name="PreauthorizationInfo" 
    component={PreauthorizationInfoScreen}
    options={{ 
        title: 'Как работает оплата',
        presentation: 'modal'
    }}
/>
```

### Обновление push-навигации:
```javascript
// В главном навигаторе добавить:
const navigateToOrderChoice = (data) => {
    navigation.navigate('OrderChoice', {
        choiceId: data.choiceId,
        orderId: data.orderId,
        fromNotification: true
    });
};

// Передать в PushNotificationService
PushNotificationService.setNavigationFunctions(
    navigateToStops,
    navigateToOrder,
    navigateToChat,
    navigateToUrl,
    navigateToOrderChoice // ← Новый обработчик
);
```

## 📊 Статистика и аналитика

### Новые метрики для отслеживания:
- Количество созданных предложений выбора
- Процент принятых/отклоненных предложений
- Среднее время ответа клиентов
- Популярность типов альтернатив
- Конверсия после предложений

### События для аналитики:
```javascript
// Добавить tracking событий:
Analytics.track('order_choice_shown', {
    choiceType: choice.choiceType,
    alternativesCount: choice.alternatives.length,
    orderId: choice.orderId
});

Analytics.track('order_choice_responded', {
    choiceId,
    response: 'ACCEPTED',
    selectedAlternative: alternativeType,
    responseTime: Date.now() - choice.createdAt
});
```

## 🎯 Преимущества для клиентов

### 🔍 **Прозрачность**
- ✅ Всегда понятно, что происходит с заказом
- ✅ Четкие объяснения проблем и решений
- ✅ Визуальные индикаторы статусов

### ⚡ **Скорость**
- ✅ Мгновенные уведомления о проблемах
- ✅ Быстрый выбор через удобный UI
- ✅ Автоматическая обработка решений

### 🛡️ **Безопасность**
- ✅ Предавторизация - деньги списываются только за доставленный товар
- ✅ Полная информация о процессе
- ✅ Возможность отмены на любом этапе

### 🎛️ **Контроль**
- ✅ Клиент всегда может выбрать подходящий вариант
- ✅ Нет принудительных решений
- ✅ Гибкие альтернативы для любой ситуации

## 🚀 Готовность к релизу

### ✅ **Завершенные компоненты:**
1. **API интеграция** - полная поддержка новых endpoint'ов
2. **UI компоненты** - адаптивные экраны и компоненты
3. **Push-уведомления** - интеграция с существующей системой
4. **Навигация** - seamless переходы между экранами
5. **Предавторизация** - прозрачная интеграция в checkout

### 📱 **Новые экраны:**
- `OrderChoiceScreen` - выбор альтернатив
- `PreauthorizationInfoScreen` - информация о платежах

### 🧩 **Новые компоненты:**
- `OrderChoiceCard` - карточка предложения
- `ChoiceNotificationBanner` - баннер уведомлений  
- `WaitingStockIndicator` - индикатор ожидания
- `WaitingStockBadge` - компактный бейдж статуса

### 🔔 **Уведомления:**
- Push-уведомления о предложениях выбора
- Напоминания о сроках ответа
- Badge счетчик активных предложений
- Визуальные индикаторы в UI

## 🎊 Результат

Мобильное приложение теперь обеспечивает:

1. **🍦 Гарантию качества мороженого** - контроль логистических ограничений
2. **💸 Финансовую безопасность** - предавторизация платежей  
3. **📞 Отличный клиентский опыт** - прозрачность и выбор
4. **🤖 Автоматизацию** - умные предложения решений
5. **⚡ Скорость** - мгновенные уведомления и быстрые решения

**Система готова к продакшену и обеспечивает premium уровень сервиса! 🚀**
