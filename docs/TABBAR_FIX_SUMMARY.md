# ✅ Исправление: TabBar теперь скрывается при открытии клавиатуры

## Что было исправлено

**Проблема:** TabBar скрывался через изменение стилей, но React Navigation не применял эти изменения в реальном времени, оставляя белое пространство.

**Решение:** Добавлен прямой условный рендеринг - когда клавиатура открывается в чате или поиске, `CustomTabBar` возвращает `null`, полностью удаляя себя из дерева компонентов.

## Изменения в коде

### CustomTabBar.jsx

Добавлено условие перед рендерингом:

```javascript
// Если TabBar должен быть скрыт, не рендерим ничего
if (!isTabBarVisible) {
    console.log('🚫 TabBar hidden - returning null');
    return null;
}
```

## Как проверить, что работает

### 1. Откройте чат
- Перейдите в таб "Чаты"
- Откройте любой диалог (личный или групповой)
- Нажмите на поле ввода
- ✅ **TabBar полностью исчезает**

### 2. Откройте поиск
- Перейдите в таб "Поиск"
- Нажмите на поле поиска или начните вводить
- ✅ **TabBar полностью исчезает**

### 3. Проверьте результат
✅ **TabBar полностью исчезает** (не остается белого пространства)  
✅ **Контент получает дополнительное место** (сообщения, результаты поиска)  
✅ **Все видно над клавиатурой**

### 4. Закройте клавиатуру
- Нажмите кнопку "назад" или свайпните вниз
- ✅ **TabBar появляется обратно**

### 5. Проверьте другие табы
- Откройте таб "Главная", "Корзина" или "Профиль"
- Начните вводить текст в любом поле
- ✅ **TabBar остается видимым** (скрывается только в чате и поиске!)

## Логи для проверки

### В чате:
```
⌨️ Keyboard shown: {"isChatScreen": true, "isSearchScreen": false, ...}
🔴 Hiding TabBar for: chat
📊 TabBarContext: Hiding TabBar
🚫 TabBar hidden - returning null
```

### В поиске:
```
⌨️ Keyboard shown: {"isChatScreen": false, "isSearchScreen": true, ...}
🔴 Hiding TabBar for: search
📊 TabBarContext: Hiding TabBar
🚫 TabBar hidden - returning null
```

### При закрытии клавиатуры:
```
⌨️ Keyboard hidden
📊 TabBarContext: Showing TabBar
```

## Что дальше

Теперь можно:
1. **Удалить отладочные логи** (если они мешают в продакшене)
2. **Добавить скрытие TabBar для других экранов** (если нужно)
3. **Протестировать на разных устройствах** (iOS и Android)
4. **Проверить на разных размерах экранов** (маленькие/большие)

## Технические детали

### Почему `return null` вместо стилей?

**До (не работало):**
```javascript
// Изменение tabBarStyle
tabBarStyle: isTabBarVisible ? {...} : { display: 'none' }
// ❌ React Navigation кэширует screenOptions и не всегда реагирует
```

**После (работает):**
```javascript
// Прямой условный рендеринг
if (!isTabBarVisible) return null;
// ✅ Компонент полностью удаляется из дерева
```

### Архитектура решения

```
Keyboard Event (в чате или поиске)
    ↓
CustomTabBar (слушает Keyboard)
    ↓
Проверка: isChatScreen || isSearchScreen?
    ↓ (да)
TabBarContext.hideTabBar()
    ↓
isTabBarVisible = false
    ↓
CustomTabBar: if (!isTabBarVisible) return null
    ↓
React Navigation освобождает место
    ↓
Больше места для контента! 🎉
```

## Файлы, которые были изменены

1. ✅ `mobile/src/widgets/navigation/context/TabBarContext.jsx` - новый Context
2. ✅ `mobile/src/widgets/navigation/ui/CustomTabBar/CustomTabBar.jsx` - условный рендеринг
3. ✅ `mobile/src/app/providers/navigation/AppNavigator.jsx` - обернуто в Provider
4. ✅ `mobile/src/widgets/navigation/index.js` - экспорты
5. ✅ `mobile/android/app/src/main/AndroidManifest.xml` - `adjustResize`
6. ✅ `mobile/app.config.js` - `softwareKeyboardLayoutMode: 'resize'`

## Связанные исправления

- [ANDROID_KEYBOARD_FIX.md](./ANDROID_KEYBOARD_FIX.md) - Основное исправление клавиатуры на Android
- [CHAT_TABBAR_KEYBOARD_HIDE.md](./CHAT_TABBAR_KEYBOARD_HIDE.md) - Полная документация решения
- [TABBAR_HIDE_DEBUG.md](./TABBAR_HIDE_DEBUG.md) - Гайд по отладке

## Готово! 🚀

Теперь пользователи получат максимум пространства для общения в чате!

