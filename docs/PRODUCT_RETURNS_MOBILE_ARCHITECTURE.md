# Архитектура системы возврата товаров - Mobile

## 📱 Структура размещения функционала

### 🔹 Для Поставщиков (SUPPLIER)

#### Где находится:
- **ProfileTab** → **SupplierScreen** → Новая карточка "Мои залежавшиеся товары"

#### Что видят:
1. **Карточка в SupplierScreen:**
   - Счетчик залежавшихся товаров
   - Уровни тревожности (CRITICAL, HIGH, MEDIUM)
   - Быстрый переход к списку

2. **Экран StagnantProductsScreen (для поставщика):**
   - Только свои товары без продаж
   - Фильтры: по складу, по количеству дней
   - Информация о каждом товаре:
     - Название, фото
     - Дней без продаж
     - Количество на складах
     - Уровень критичности
   - Действие: "Посмотреть детали товара"

3. **Экран ProductReturnsListScreen (для поставщика):**
   - Список возвратов по их товарам
   - Статусы: PENDING, APPROVED, IN_PROGRESS, COMPLETED, REJECTED
   - Фильтры по статусу
   - Детали каждого возврата

4. **Уведомления:**
   - Push о товарах без продаж 21+ дней
   - Push о критических товарах 30+ дней
   - Уведомления о новых запросах на возврат
   - Уведомления об одобрении возврата

---

### 🔹 Для Админов и Сотрудников (ADMIN, EMPLOYEE)

#### Где находится:
- **ProfileTab** → **Admin/AdminPanel** → Новый раздел "Управление возвратами"

#### Что видят:

1. **AdminPanelScreen - новый раздел:**
   ```
   📦 Управление возвратами товаров
   ├─ Залежавшиеся товары (с бейджем количества)
   ├─ Создать запрос на возврат
   ├─ Запросы на возврат (активные)
   └─ История и статистика
   ```

2. **Экран StagnantProductsScreen (для админа/сотрудника):**
   - ВСЕ залежавшиеся товары в системе
   - Фильтры:
     - По поставщику
     - По складу
     - По количеству дней (21+, 30+)
     - По уровню критичности
   - Группировка:
     - По поставщикам
     - По складам
     - По критичности
   - Для каждого товара:
     - Название, фото, поставщик
     - Дней без продаж
     - Количество и склады
     - Уровень критичности
     - Действия:
       - "Создать запрос на возврат"
       - "Посмотреть детали"
       - "Связаться с поставщиком"

3. **Экран CreateReturnRequestScreen:**
   - Форма создания запроса на возврат:
     - Выбор товара (из залежавшихся)
     - Выбор склада
     - Количество коробок для возврата
     - Причина возврата
     - Дополнительные заметки
   - Автозаполнение данных из залежавшегося товара

4. **Экран ProductReturnsListScreen (для админа/сотрудника):**
   - ВСЕ запросы на возврат в системе
   - Фильтры:
     - По статусу (PENDING, APPROVED, IN_PROGRESS, COMPLETED, REJECTED)
     - По поставщику
     - По складу
     - По дате создания
   - Для каждого возврата:
     - Товар, фото
     - Поставщик
     - Склад
     - Количество
     - Статус с цветовой индикацией
     - Дата создания
     - Действия (зависят от роли и статуса)

5. **Экран ProductReturnDetailScreen:**
   - Детальная информация о возврате:
     - Информация о товаре (фото, название, артикул)
     - Поставщик (с кнопкой "Связаться")
     - Склад
     - Количество коробок
     - Причина возврата
     - История статусов (timeline)
     - Дополнительные заметки
   - Действия (только для ADMIN):
     - PENDING → "Одобрить" / "Отклонить"
     - APPROVED → "Начать процесс возврата"
     - IN_PROGRESS → "Завершить возврат"
   - Действия (для EMPLOYEE):
     - "Начать процесс возврата" (если APPROVED)
     - "Завершить возврат" (если IN_PROGRESS)

6. **Экран ReturnStatisticsScreen:**
   - Общая статистика:
     - Всего возвратов
     - По статусам (диаграмма)
     - Количество возвращенных коробок
     - Топ поставщиков по возвратам
     - Топ товаров по возвратам
   - Фильтры по периоду
   - Графики динамики

7. **Уведомления:**
   - Новые залежавшиеся товары (21+ дней)
   - Критические товары (30+ дней)
   - Новые запросы на возврат
   - Изменения статусов возвратов

---

## 🎨 UI/UX Концепция

### Цветовая кодировка уровней критичности:

```javascript
const URGENCY_COLORS = {
  CRITICAL: {
    color: '#FF3B30',    // Красный
    bg: '#FFEBEE',
    icon: '🔴',
    label: 'Критично (30+ дней)'
  },
  HIGH: {
    color: '#FF9500',    // Оранжевый
    bg: '#FFF3E0',
    icon: '🟠',
    label: 'Требует внимания (21-29 дней)'
  },
  MEDIUM: {
    color: '#FFCC00',    // Желтый
    bg: '#FFF9C4',
    icon: '🟡',
    label: 'Наблюдение (<21 дней)'
  }
};
```

### Цветовая кодировка статусов возврата:

```javascript
const STATUS_COLORS = {
  PENDING: {
    color: '#007AFF',    // Синий
    bg: '#E3F2FD',
    icon: '⏳',
    label: 'Ожидает рассмотрения'
  },
  APPROVED: {
    color: '#34C759',    // Зеленый
    bg: '#E8F5E9',
    icon: '✅',
    label: 'Одобрено'
  },
  IN_PROGRESS: {
    color: '#FF9500',    // Оранжевый
    bg: '#FFF3E0',
    icon: '🔄',
    label: 'В процессе'
  },
  COMPLETED: {
    color: '#5856D6',    // Фиолетовый
    bg: '#EDE7F6',
    icon: '✨',
    label: 'Завершено'
  },
  REJECTED: {
    color: '#FF3B30',    // Красный
    bg: '#FFEBEE',
    icon: '❌',
    label: 'Отклонено'
  },
  CANCELLED: {
    color: '#8E8E93',    // Серый
    bg: '#F5F5F5',
    icon: '🚫',
    label: 'Отменено'
  }
};
```

---

## 📂 Структура файлов

```
mobile/src/
├── entities/
│   └── product-return/
│       ├── api/
│       │   └── productReturnApi.js
│       ├── model/
│       │   ├── slice.js
│       │   ├── selectors.js
│       │   └── types.js
│       └── ui/
│           ├── StagnantProductCard.jsx
│           ├── ProductReturnCard.jsx
│           ├── ReturnStatusBadge.jsx
│           └── UrgencyLevelBadge.jsx
│
├── features/
│   ├── product-return-management/
│   │   ├── create-return/
│   │   │   ├── ui/
│   │   │   │   └── CreateReturnForm.jsx
│   │   │   └── model/
│   │   │       └── useCreateReturn.js
│   │   ├── manage-return/
│   │   │   ├── ui/
│   │   │   │   ├── ApproveReturnButton.jsx
│   │   │   │   ├── RejectReturnButton.jsx
│   │   │   │   └── CompleteReturnButton.jsx
│   │   │   └── model/
│   │   │       └── useManageReturn.js
│   │   └── stagnant-products/
│   │       ├── ui/
│   │       │   ├── StagnantProductsList.jsx
│   │       │   └── StagnantProductsFilters.jsx
│   │       └── model/
│   │           └── useStagnantProducts.js
│   │
│   └── supplier-returns/
│       ├── ui/
│       │   ├── SupplierReturnsList.jsx
│       │   └── SupplierStagnantProducts.jsx
│       └── model/
│           └── useSupplierReturns.js
│
├── widgets/
│   ├── product-return/
│   │   ├── StagnantProductsWidget.jsx    # Виджет для админ-панели
│   │   ├── SupplierReturnsWidget.jsx     # Виджет для поставщика
│   │   └── ReturnStatisticsWidget.jsx
│   │
│   └── admin/
│       └── AdminReturnsSection.jsx        # Секция в AdminPanelScreen
│
└── screens/
    ├── product-return/
    │   ├── StagnantProductsScreen.jsx      # Список залежавшихся товаров
    │   ├── ProductReturnsListScreen.jsx    # Список возвратов
    │   ├── ProductReturnDetailScreen.jsx   # Детали возврата
    │   ├── CreateReturnRequestScreen.jsx   # Создание запроса
    │   └── ReturnStatisticsScreen.jsx      # Статистика
    │
    └── supplier/
        └── SupplierReturnsScreen.jsx        # Экран возвратов поставщика
```

---

## 🔄 Навигация

### Для поставщика:

```
ProfileTab 
  → SupplierScreen 
    → (Карточка "Мои залежавшиеся товары") 
      → StagnantProductsScreen (supplierId)
    → (Карточка "Мои возвраты") 
      → ProductReturnsListScreen (supplierId)
        → ProductReturnDetailScreen (returnId)
          → ProductDetail (productId)
```

### Для админа/сотрудника:

```
ProfileTab 
  → Admin/AdminPanel 
    → (Раздел "Управление возвратами")
      → StagnantProductsScreen (all)
        → CreateReturnRequestScreen (productId, warehouseId)
        → ProductDetail (productId)
      
      → ProductReturnsListScreen (all)
        → ProductReturnDetailScreen (returnId)
          → ApproveReturn / RejectReturn
          → CompleteReturn
          → ProductDetail (productId)
          → SupplierScreen (supplierId)
      
      → ReturnStatisticsScreen
```

---

## 🔔 Уведомления

### Типы уведомлений:

```javascript
const NOTIFICATION_TYPES = {
  // Для всех ролей
  STAGNANT_PRODUCT_WARNING: {
    type: 'STAGNANT_PRODUCT',
    level: 'WARNING',
    days: 21,
    navigate: 'StagnantProductsScreen'
  },
  STAGNANT_PRODUCT_CRITICAL: {
    type: 'STAGNANT_PRODUCT',
    level: 'CRITICAL',
    days: 30,
    navigate: 'StagnantProductsScreen'
  },
  
  // Для поставщика
  RETURN_REQUESTED: {
    type: 'RETURN_REQUESTED',
    navigate: 'ProductReturnDetailScreen'
  },
  RETURN_APPROVED: {
    type: 'RETURN_APPROVED',
    navigate: 'ProductReturnDetailScreen'
  },
  RETURN_REJECTED: {
    type: 'RETURN_REJECTED',
    navigate: 'ProductReturnDetailScreen'
  },
  RETURN_COMPLETED: {
    type: 'RETURN_COMPLETED',
    navigate: 'ProductReturnDetailScreen'
  },
  
  // Для админа/сотрудника
  NEW_RETURN_REQUEST: {
    type: 'NEW_RETURN_REQUEST',
    navigate: 'ProductReturnDetailScreen'
  },
  RETURN_STATUS_CHANGED: {
    type: 'RETURN_STATUS_CHANGED',
    navigate: 'ProductReturnDetailScreen'
  }
};
```

---

## 🎯 Приоритеты реализации

### Фаза 1 - Базовый функционал (MVP):
1. ✅ API интеграция
2. ✅ Entities (productReturn)
3. ✅ Виджет для AdminPanelScreen
4. ✅ Виджет для SupplierScreen
5. ✅ StagnantProductsScreen (базовый список)
6. ✅ ProductReturnsListScreen (базовый список)

### Фаза 2 - Управление возвратами:
7. ✅ CreateReturnRequestScreen
8. ✅ ProductReturnDetailScreen
9. ✅ Действия: одобрить, отклонить, завершить
10. ✅ Уведомления (интеграция с существующей системой)

### Фаза 3 - Расширенный функционал:
11. ✅ Фильтры и сортировка
12. ✅ ReturnStatisticsScreen
13. ✅ Графики и аналитика
14. ✅ Экспорт данных

---

## 💡 Дополнительные возможности

1. **Быстрые действия:**
   - Свайп для быстрого создания возврата
   - Long press для дополнительных опций

2. **Офлайн режим:**
   - Кэширование списка возвратов
   - Отложенная отправка действий

3. **Интеграция с чатами:**
   - Быстрый переход в чат с поставщиком из экрана возврата
   - Отправка уведомлений о возврате в чат

4. **Сканер:**
   - QR/Barcode сканирование товара для быстрого создания возврата

---

## 🔐 Права доступа

```javascript
const PERMISSIONS = {
  // Поставщик
  SUPPLIER: {
    view: ['own_stagnant_products', 'own_returns'],
    create: [],
    approve: [],
    reject: [],
    complete: []
  },
  
  // Сотрудник
  EMPLOYEE: {
    view: ['all_stagnant_products', 'all_returns'],
    create: ['return_request'],
    approve: [],
    reject: [],
    complete: ['return']
  },
  
  // Админ
  ADMIN: {
    view: ['all_stagnant_products', 'all_returns', 'statistics'],
    create: ['return_request'],
    approve: ['return'],
    reject: ['return'],
    complete: ['return']
  }
};
```

---

Готово! Теперь начнем реализацию? 🚀

