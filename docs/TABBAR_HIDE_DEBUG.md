# Отладка скрытия TabBar при открытии клавиатуры

## Добавлена детальная отладка

Для диагностики проблемы со скрытием TabBar добавлено подробное логирование на всех уровнях.

## Логи, которые нужно проверить

### 1. При открытии клавиатуры в чате

Вы должны увидеть следующую последовательность логов:

```
🔍 Route structure: {
  currentRouteName: "ChatList",
  hasState: true,
  stateRoutes: ["ChatMain", "ChatRoom"],
  nestedRoutes: [...]
}

⌨️ Keyboard shown: {
  currentTab: "ChatList",
  isChatTab: true,
  hasChatRoom: true,
  hasNestedChatRoom: false,
  isChatScreen: true,
  keyboardHeight: 336
}

🔴 Hiding TabBar for chat screen

📊 TabBarContext: Hiding TabBar

🎯 TabBar visibility changed: false

📐 TabBar style: {
  display: 'none',
  position: 'absolute',
  bottom: -100
}
```

### 2. При закрытии клавиатуры

```
⌨️ Keyboard hidden

📊 TabBarContext: Showing TabBar

🎯 TabBar visibility changed: true

📐 TabBar style: {
  elevation: 8,
  shadowOpacity: 0.1,
  shadowRadius: 6,
  shadowOffset: { width: 0, height: -2 }
}
```

## Возможные проблемы и решения

### Проблема 1: "isChatScreen: false" или "isSearchScreen: false" хотя вы в чате/поиске

**Симптом:** В логах видно `isChatScreen: false` и `isSearchScreen: false`, хотя вы находитесь в чате или поиске

**Причина:** Структура маршрутов не соответствует ожидаемой

**Решение:** Проверьте лог `Route structure` и найдите правильное имя маршрута. Обновите проверку в `CustomTabBar.jsx`:

```javascript
// Для чата:
const isChatTab = currentRoute?.name === 'ИМЯ_ВАШЕГО_ЧАТА';

// Для поиска:
const isSearchTab = currentRoute?.name === 'ИМЯ_ВАШЕГО_ПОИСКА';
```

### Проблема 2: Context не обновляется

**Симптом:** Видно "Hiding TabBar", но не видно "TabBar visibility changed"

**Причина:** TabBarProvider не обернул навигатор

**Решение:** Убедитесь, что в `AppNavigator.jsx` есть:

```javascript
export const MainTabNavigator = () => (
    <TabBarProvider>
        <MainTabNavigatorContent />
    </TabBarProvider>
);
```

### Проблема 3: TabBar style не применяется

**Симптом:** Видно "TabBar visibility changed: false", но TabBar остается видимым

**Причина:** React Navigation кэширует screenOptions

**Попробуйте:**

1. Полностью перезапустить приложение (не hot reload):
   ```bash
   npm start -- --reset-cache
   ```

2. Использовать `tabBarVisible` вместо `tabBarStyle`:
   ```javascript
   screenOptions={({ route }) => ({
       tabBarVisible: isTabBarVisible,
       // ...
   })}
   ```

### Проблема 4: TabBar скрывается, но место остается

**Симптом:** TabBar исчезает, но белое пространство внизу остается

**Причина:** `display: 'none'` не освобождает пространство в React Navigation

**Текущее решение:** Добавлено `position: 'absolute', bottom: -100` для смещения TabBar за пределы экрана.

**Альтернатива:** Использовать полностью кастомный рендеринг через условный рендеринг в `tabBar`:

```javascript
tabBar={props => isTabBarVisible ? <CustomTabBar {...props} /> : null}
```

## Команды для тестирования

### 1. Проверка в разных экранах

Откройте клавиатуру в следующих местах и проверьте логи:

- ✅ **ChatList > ChatRoom** (должен скрывать)
- ✅ **ChatList > ChatMain** (должен скрывать)  
- ✅ **Search > SearchMain** (должен скрывать)
- ❌ **MainTab** (НЕ должен скрывать)
- ❌ **Cart > CartMain** (НЕ должен скрывать)
- ❌ **ProfileTab** (НЕ должен скрывать)

### 2. Фильтр логов в терминале

Для удобства можно фильтровать логи:

**Только TabBar логи:**
```bash
# На Windows в PowerShell
npm start | Select-String "TabBar|⌨️|🔍"
```

**На Mac/Linux:**
```bash
npm start | grep -E "TabBar|⌨️|🔍"
```

### 3. Проверка на разных платформах

- **iOS:** Должна использовать `keyboardWillShow/Hide`
- **Android:** Должна использовать `keyboardDidShow/Hide`

## ✅ РЕШЕНИЕ НАЙДЕНО

**Проблема:** React Navigation не применяет динамические изменения `tabBarStyle` в реальном времени, даже когда context обновляется корректно.

**Решение:** Использовать прямой условный рендеринг в `CustomTabBar`:

```javascript
export const CustomTabBar = ({ state, descriptors, navigation }) => {
    const { isTabBarVisible } = useTabBar();
    
    // Если TabBar должен быть скрыт, не рендерим ничего
    if (!isTabBarVisible) {
        console.log('🚫 TabBar hidden - returning null');
        return null;
    }
    
    return (
        <View style={styles.menuDoneWithBack}>
            {/* ... контент */}
        </View>
    );
};
```

### Почему это работает?

1. **Прямой контроль:** `return null` полностью удаляет TabBar из дерева компонентов
2. **Освобождение пространства:** React Navigation автоматически освобождает место, когда `tabBar` возвращает `null`
3. **Быстрая реакция:** Нет задержек, связанных с применением стилей
4. **Надежность:** Работает на всех версиях React Navigation

### Альтернатива (если нужна анимация)

Если требуется плавная анимация появления/скрытия, можно использовать условный рендеринг в `tabBar` prop:

```javascript
const MainTabNavigatorContent = () => {
    const { isTabBarVisible } = useTabBar();
    
    return (
        <Tab.Navigator
            tabBar={props => isTabBarVisible ? <CustomTabBar {...props} /> : null}
            // ...
        >
```

Но текущее решение через условный рендеринг внутри `CustomTabBar` проще и эффективнее.

## Следующие шаги

1. **Запустите приложение** с текущими изменениями
2. **Откройте чат** и начните печатать
3. **Проверьте консоль** на наличие всех логов выше
4. **Скопируйте логи** и отправьте разработчику для анализа

## Контрольный список

- [ ] Логи показывают `isChatScreen: true` в чате
- [ ] Видно "Hiding TabBar for chat screen"
- [ ] Видно "TabBarContext: Hiding TabBar"
- [ ] Видно "TabBar visibility changed: false"
- [ ] Видно "TabBar style: { display: 'none', ... }"
- [ ] TabBar визуально исчезает
- [ ] Белое пространство под TabBar исчезает
- [ ] При закрытии клавиатуры TabBar возвращается
- [ ] В других табах TabBar не скрывается при вводе текста

