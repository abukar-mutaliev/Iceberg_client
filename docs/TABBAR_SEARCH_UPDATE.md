# ✅ Обновление: TabBar теперь скрывается и в поиске

## Что добавлено

Расширена функциональность автоматического скрытия TabBar - теперь он скрывается не только в чате, но и **в экране поиска** при открытии клавиатуры.

## Зачем это нужно?

### В поиске (SearchScreen):
- Пользователь активно вводит поисковые запросы
- Появляется клавиатура, которая занимает ~40% экрана
- Нужно больше места для:
  - ✅ Просмотра результатов поиска
  - ✅ Истории поиска
  - ✅ Популярных тегов
  - ✅ Подсказок товаров

### Выгода:
- **На 60-80 пикселей больше** для отображения контента
- **Лучший UX** - пользователь видит больше результатов
- **Единообразие** - поведение как в чате

## Что изменилось в коде

### CustomTabBar.jsx

Добавлена проверка для экрана поиска:

```javascript
// Проверяем, находимся ли мы в поиске
const isSearchTab = currentRoute?.name === 'Search';
const hasSearchMain = currentRoute?.state?.routes?.some(route => 
    route.name === 'SearchMain'
);

const isSearchScreen = isSearchTab || hasSearchMain;

// Скрываем TabBar для чата ИЛИ поиска
if (isChatScreen || isSearchScreen) {
    console.log('🔴 Hiding TabBar for:', isChatScreen ? 'chat' : 'search');
    hideTabBar();
}
```

## Поддерживаемые экраны

TabBar теперь **автоматически скрывается** в:

| Экран | Скрывается? | Причина |
|-------|------------|---------|
| **ChatList** | ✅ Да | Часто открывается клавиатура |
| **ChatRoom** (личные) | ✅ Да | Постоянный ввод сообщений |
| **GroupChatScreen** | ✅ Да | Постоянный ввод сообщений |
| **SearchScreen** | ✅ Да | Активный поиск |
| **SearchMain** | ✅ Да | Ввод поисковых запросов |
| MainTab | ❌ Нет | Редко используется ввод |
| Cart | ❌ Нет | Редко используется ввод |
| Favourites | ❌ Нет | Редко используется ввод |
| ProfileTab | ❌ Нет | Редко используется ввод |

## Как это работает

### Пошаговый процесс:

1. **Пользователь открывает поиск** → Переходит в таб "Search"
2. **Нажимает на поле поиска** → Открывается клавиатура
3. **Keyboard Event детектируется** → `CustomTabBar` получает событие
4. **Проверка маршрута:** 
   - `currentRoute.name === 'Search'` → ✅ true
   - `isSearchScreen = true`
5. **Вызов hideTabBar()** → Context обновляется
6. **isTabBarVisible = false** → TabBar получает обновление
7. **Условный рендеринг:** `if (!isTabBarVisible) return null`
8. **TabBar исчезает** → React Navigation освобождает место
9. **Результаты поиска получают больше места** 🎉

### При закрытии клавиатуры:

1. **Keyboard Hidden Event** → `CustomTabBar` получает событие
2. **showTabBar()** → Context обновляется
3. **isTabBarVisible = true** → TabBar рендерится снова
4. **TabBar появляется обратно** ✅

## Тестирование

### Сценарий 1: Поиск с клавиатурой
```
1. Откройте приложение
2. Перейдите в таб "Поиск" (иконка лупы)
3. Нажмите на поле поиска
   → ✅ Клавиатура открылась
   → ✅ TabBar полностью исчез
   → ✅ Видно больше результатов/истории
4. Начните вводить текст
   → ✅ TabBar остается скрытым
5. Нажмите "Назад" или свайпните клавиатуру вниз
   → ✅ TabBar появился обратно
```

### Сценарий 2: История поиска
```
1. В поиске, нажмите на поле ввода
   → ✅ Открывается история поиска
   → ✅ TabBar скрыт
   → ✅ Видно больше элементов истории
```

### Сценарий 3: Результаты поиска
```
1. Введите поисковый запрос
2. Появляются подсказки товаров
   → ✅ TabBar скрыт
   → ✅ Видно больше товаров
```

### Сценарий 4: Переключение табов
```
1. Откройте поиск, начните печатать (TabBar скрыт)
2. Переключитесь на "Главная"
   → ✅ TabBar появился обратно
3. Откройте любое поле ввода на главной
   → ✅ TabBar остается видимым (это правильно!)
```

## Логи для проверки

При открытии клавиатуры в поиске вы увидите:

```log
🔍 Route structure: {
  "currentRouteName": "Search",
  "hasState": true,
  "stateRoutes": ["SearchMain"],
  ...
}

⌨️ Keyboard shown: {
  "currentTab": "Search",
  "isChatScreen": false,
  "isSearchScreen": true,  ← ✅ TRUE!
  "keyboardHeight": 336
}

🔴 Hiding TabBar for: search  ← ✅ Правильная причина

📊 TabBarContext: Hiding TabBar

🚫 TabBar hidden - returning null

📐 TabBar style: {
  "display": "none",
  "position": "absolute",
  "bottom": -100
}

🎯 TabBar visibility changed: false
```

## Возможные проблемы

### Проблема: TabBar не скрывается в поиске

**Проверьте:**
1. Логи - видите ли `isSearchScreen: true`?
2. Имя маршрута совпадает с `'Search'` или `'SearchMain'`?

**Решение:** Проверьте структуру навигации:
```javascript
// Добавьте временный лог в CustomTabBar.jsx:
console.log('Current route name:', currentRoute?.name);
console.log('State routes:', currentRoute?.state?.routes?.map(r => r.name));
```

### Проблема: TabBar скрывается в других табах

**Симптом:** В Главной/Корзине TabBar исчезает при вводе

**Причина:** Слишком широкая проверка `isSearchScreen`

**Решение:** Убедитесь, что проверка строгая:
```javascript
const isSearchTab = currentRoute?.name === 'Search'; // Точное совпадение!
```

## Файлы, которые были изменены

1. ✅ `mobile/src/widgets/navigation/ui/CustomTabBar/CustomTabBar.jsx`
   - Добавлена проверка `isSearchScreen`
   - Обновлена логика скрытия `if (isChatScreen || isSearchScreen)`
   - Добавлены логи для отладки

2. ✅ `mobile/docs/CHAT_TABBAR_KEYBOARD_HIDE.md`
   - Обновлена документация - теперь включает поиск
   - Добавлены новые сценарии тестирования

3. ✅ `mobile/docs/TABBAR_FIX_SUMMARY.md`
   - Обновлены примеры и логи
   - Добавлена информация о поиске

4. ✅ `mobile/docs/TABBAR_HIDE_DEBUG.md`
   - Обновлена секция отладки
   - Добавлены проверки для поиска

## Сравнение: До и После

### До (только чат):
```
Чат: TabBar скрывается ✅
Поиск: TabBar виден ❌ (мало места для результатов)
```

### После (чат + поиск):
```
Чат: TabBar скрывается ✅
Поиск: TabBar скрывается ✅ (больше места!)
Другие: TabBar виден ✅ (не мешает)
```

## Производительность

- **Нет влияния на производительность** - используется только условный рендеринг
- **Быстрая реакция** - TabBar исчезает моментально вместе с клавиатурой
- **Оптимизировано** - проверка маршрутов очень быстрая

## Следующие шаги

Если нужно скрыть TabBar в других экранах:

```javascript
// Пример: скрыть в корзине
const isCartTab = currentRoute?.name === 'Cart';
const hasCartMain = currentRoute?.state?.routes?.some(route => 
    route.name === 'CartMain'
);
const isCartScreen = isCartTab || hasCartMain;

// Обновить условие:
if (isChatScreen || isSearchScreen || isCartScreen) {
    hideTabBar();
}
```

## Готово! 🚀

Теперь пользователи получат лучший опыт как в чате, так и в поиске - больше места для того, что важно!

---

**Связанные документы:**
- [CHAT_TABBAR_KEYBOARD_HIDE.md](./CHAT_TABBAR_KEYBOARD_HIDE.md) - Полная техническая документация
- [TABBAR_FIX_SUMMARY.md](./TABBAR_FIX_SUMMARY.md) - Краткое резюме исправления
- [TABBAR_HIDE_DEBUG.md](./TABBAR_HIDE_DEBUG.md) - Руководство по отладке


