# Исправление проблемы "слетания" авторизации

## 🔍 Проблема

Пользователи жаловались, что авторизация "слетает" через какое-то время, хотя они не выходили из аккаунта.

## 📋 Найденные причины

### 1. **Auth state не сохранялся в Redux Persist**
- В `store.js` whitelist содержал только `['favorites']`
- Auth state терялся при перезапуске приложения
- Токены хранились в AsyncStorage отдельно, но Redux state был пустой

### 2. **Рассинхронизация между AsyncStorage и Redux**
- Токены обновлялись в AsyncStorage через API interceptor
- Redux store не обновлялся при автоматическом refresh токенов
- При перезапуске приложения: токены восстанавливались, но Redux показывал `isAuthenticated: false`

### 3. **Отсутствие обновления Redux после восстановления токенов**
- В `App.jsx` токены восстанавливались из AsyncStorage
- Но dispatch `setTokens` не вызывался для обновления Redux

### 4. **Недостаточное логирование**
- Сложно было отследить где именно происходит потеря авторизации

## ✅ Внесенные исправления

### 1. **Добавлен auth в Redux Persist** (`store.js`)
```javascript
whitelist: ['auth', 'favorites'], // Добавлен auth
version: 2, // Увеличена версия для миграции
```

**Эффект**: Auth state теперь сохраняется между перезапусками приложения.

### 2. **Обновление Redux при восстановлении токенов** (`App.jsx`)
```javascript
// После refresh токенов
dispatch({ 
    type: 'auth/setTokens', 
    payload: { 
        accessToken: refreshed.accessToken, 
        refreshToken: refreshed.refreshToken 
    } 
});

// При валидных токенах синхронизируем Redux
dispatch({ 
    type: 'auth/setTokens', 
    payload: { 
        accessToken: tokens.accessToken, 
        refreshToken: tokens.refreshToken 
    } 
});
```

**Эффект**: Redux store синхронизируется с токенами из AsyncStorage при запуске.

### 3. **Улучшено обновление Redux в api.js**
```javascript
export const handleAuthTokens = (tokens) => {
    console.log('🔑 [API] handleAuthTokens called');
    if (dispatchAction) {
        dispatchAction({ type: 'auth/setTokens', payload: tokens });
        console.log('✅ [API] Tokens dispatched to Redux store');
    } else {
        console.warn('⚠️ [API] dispatchAction not available');
    }
};
```

**Эффект**: Теперь видно когда токены обновляются и почему Redux может не обновиться.

### 4. **Добавлено подробное логирование**

Добавлено логирование в критические точки:
- ✅ `slice.js` - setTokens reducer
- ✅ `slice.js` - login/logout/register/refreshToken actions
- ✅ `api.js` - handleAuthTokens, handleRefreshToken
- ✅ `api.js` - request interceptor (проактивный refresh)
- ✅ `api.js` - authService.refreshAccessToken

**Эффект**: Полная видимость процесса авторизации и обновления токенов.

## 🔧 Технические детали

### Время жизни токенов (сервер)
- **Access Token**: 1 час
- **Refresh Token**: 30 дней

### Механизм обновления токенов

1. **Проактивное обновление** (Request Interceptor):
   - Перед каждым запросом проверяется срок действия access token
   - Если истек, но refresh token валиден - автоматически обновляется
   - Все ожидающие запросы получают новый токен

2. **Реактивное обновление** (Response Interceptor):
   - При получении 401 ошибки запускается refresh token
   - Если успешно - запрос повторяется с новым токеном
   - Если refresh token истек - пользователь разлогинивается

3. **Синхронизация с Redux**:
   - После любого обновления токенов вызывается `handleAuthTokens`
   - Redux store обновляется через `dispatch({ type: 'auth/setTokens' })`
   - Redux Persist автоматически сохраняет состояние

## 🧪 План тестирования

### Тест 1: Перезапуск приложения
1. Войдите в приложение
2. Закройте приложение полностью
3. Откройте снова
4. ✅ **Ожидается**: Пользователь остается авторизованным

### Тест 2: Истечение Access Token (через 1 час)
1. Войдите в приложение
2. Подождите > 1 часа
3. Совершите любое действие (откройте товар, категорию и т.д.)
4. ✅ **Ожидается**: Токен автоматически обновится, действие выполнится

### Тест 3: Долгое использование (> 24 часов)
1. Войдите в приложение
2. Используйте приложение периодически в течение нескольких дней
3. ✅ **Ожидается**: Пользователь остается авторизованным

### Тест 4: Истечение Refresh Token (через 30 дней)
1. Войдите в приложение
2. Не используйте приложение 30+ дней
3. Откройте приложение
4. ✅ **Ожидается**: Пользователь разлогинен, показан экран входа

### Тест 5: Сетевые ошибки
1. Войдите в приложение
2. Отключите интернет на 5 минут
3. Включите интернет
4. ✅ **Ожидается**: Приложение восстановит соединение, авторизация сохранится

## 📊 Логи для отладки

При возникновении проблем проверьте логи:

```
🔑 [API] handleAuthTokens called - обновление токенов в API
🔑 [REDUX] setTokens called - обновление токенов в Redux
✅ [REDUX] User authenticated - пользователь авторизован
✅ [API REQUEST] Token refreshed proactively - токен обновлен до истечения
🔄 [API] handleRefreshToken - начало обновления токена
❌ [REDUX] refreshToken rejected - ошибка обновления токена
```

## 🚀 Развертывание

### 1. Очистка кэша для пользователей
После обновления приложения пользователи могут увидеть проблемы с миграцией состояния.

**Рекомендация**: Увеличена версия persist с 1 до 2, что автоматически сбросит старое состояние.

### 2. Мониторинг
Следите за логами в течение первых дней после релиза:
- Частота автоматических refresh токенов
- Ошибки при обновлении токенов
- Проблемы с синхронизацией Redux

### 3. Откат
Если возникнут проблемы, можно откатить изменения:
1. Вернуть `whitelist: ['favorites']` в `store.js`
2. Убрать dispatch в `App.jsx`
3. Уменьшить версию persist обратно до 1

## 📝 Дополнительные улучшения

### Возможные будущие улучшения:

1. **Sliding Window для Refresh Token**:
   - ✅ Уже реализовано на сервере
   - Каждое использование refresh token продлевает сессию на новые 30 дней

2. **Биометрическая авторизация**:
   - Хранить refresh token в Secure Storage
   - Разблокировка по Face ID / Touch ID

3. **Мониторинг сессий**:
   - Dashboard для администраторов
   - Показывать активные сессии пользователей
   - Возможность удаленного logout

4. **Push уведомления о безопасности**:
   - Уведомление при входе с нового устройства
   - Предупреждение о скором истечении сессии

## 🔗 Связанные файлы

- `mobile/src/app/store/store.js` - конфигурация Redux Persist
- `mobile/src/app/App.jsx` - инициализация приложения
- `mobile/src/shared/api/api.js` - HTTP клиент и interceptors
- `mobile/src/entities/auth/model/slice.js` - Redux slice для auth
- `server/src/services/token.service.js` - серверная логика токенов
- `server/src/middlewares/auth.middleware.js` - проверка токенов на сервере

## ⚠️ Важные замечания

1. **Не удаляйте токены из AsyncStorage вручную** - это может привести к рассинхронизации
2. **При изменении структуры auth state** увеличивайте версию persist
3. **Токены не должны логироваться в production** - добавьте проверку `__DEV__`
4. **Время на клиенте должно быть синхронизировано** - иначе проверка exp может давать ложные срабатывания

## 📞 Контакты

При возникновении проблем свяжитесь с командой разработки.

---

**Дата создания**: ${new Date().toLocaleDateString('ru-RU')}  
**Версия приложения**: 1.0.0  
**Автор**: AI Assistant

