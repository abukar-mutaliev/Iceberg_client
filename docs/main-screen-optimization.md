# Оптимизация главного экрана - предотвращение лишних загрузок

## Проблема
До оптимизации главный экран перезагружал данные с сервера при каждом переходе на этот экран, что приводило к:
- Ненужным сетевым запросам
- Медленной навигации
- Излишней нагрузке на сервер
- Плохому пользовательскому опыту

## Решение

### 1. Умная система кэширования
- **Проверка существующих данных**: перед загрузкой проверяется наличие данных в Redux
- **Временные метки**: отслеживание времени последней загрузки данных
- **Интервал обновления**: данные обновляются только если прошло более 5 минут (REFRESH_INTERVAL)

### 2. Оптимизированная загрузка
```javascript
// Новая логика в loadAllData
const loadAllData = useCallback(async (refresh = false, force = false) => {
    // Если уже есть данные и это не принудительная перезагрузка
    if (!force && hasExistingData && dataLoadedRef.current && !refresh) {
        return; // Выходим без загрузки
    }
    
    // Загружаем данные только при необходимости
}, [dispatch, hasExistingData]);
```

### 3. Умный useFocusEffect
```javascript
useFocusEffect(
    useCallback(() => {
        // Проверяем сигнал о принудительном обновлении
        if (route?.params?.refreshMainScreen) {
            forceRefreshData();
            return;
        }

        // Проверяем нужно ли обновить данные (только если давно не обновлялись)
        if (isInitialized && shouldRefreshData()) {
            loadAllData(false, false);
        }
    }, [/* зависимости */])
);
```

### 4. Система принудительного обновления

#### Хук useMainScreenRefresh
Для случаев, когда другие экраны изменили данные и нужно обновить главный экран:

```javascript
import { useMainScreenRefresh } from '@entities/product';

const SomeScreen = () => {
    const { triggerMainScreenRefresh, navigateToMainWithRefresh } = useMainScreenRefresh();
    
    const handleDataChange = async () => {
        // Изменяем данные
        await updateProductData();
        
        // Сигнализируем главному экрану об обновлении
        triggerMainScreenRefresh();
    };
    
    const goBackToMain = () => {
        // Навигация на главный экран с принудительным обновлением
        navigateToMainWithRefresh();
    };
};
```

## Результат оптимизации

### ✅ Преимущества
1. **Мгновенная навигация**: данные не перезагружаются при каждом переходе
2. **Сохранение данных**: информация остается между навигацией
3. **Умное обновление**: данные обновляются только при необходимости
4. **Принудительное обновление**: возможность обновить данные из других экранов
5. **Экономия трафика**: уменьшение количества сетевых запросов

### 🔄 Сценарии обновления
1. **Первая загрузка**: данные загружаются обычным способом
2. **Навигация обратно**: данные остаются, не перезагружаются
3. **Устаревшие данные**: автоматическое обновление через 5 минут
4. **Pull-to-refresh**: принудительное обновление пользователем
5. **Изменения из других экранов**: обновление по сигналу

### 📱 Состояния экрана
- `isLoading && !hasExistingData` - показываем скелетон только при первой загрузке
- `hasExistingData` - показываем данные немедленно
- `isRefreshing` - показываем индикатор обновления при pull-to-refresh

## Технические детали

### Ключевые переменные состояния
```javascript
const [isInitialized, setIsInitialized] = useState(false);
const dataLoadedRef = useRef(false);
const hasExistingData = useMemo(() => {
    return products?.length > 0 && categories?.length > 0;
}, [products?.length, categories?.length]);
```

### Интервалы и константы
```javascript
const REFRESH_INTERVAL = 300000; // 5 минут
const CACHE_EXPIRY = 10 * 60 * 1000; // 10 минут для AsyncStorage кэша
```

### Функции управления
- `loadAllData(refresh, force)` - основная функция загрузки
- `forceRefreshData()` - принудительное обновление
- `shouldRefreshData()` - проверка необходимости обновления
- `hasExistingData` - проверка наличия данных

## Миграция и совместимость

Оптимизация полностью обратно совместима:
- Существующие экраны продолжают работать без изменений
- Добавлена возможность использовать `useMainScreenRefresh` для улучшения UX
- Все существующие функции refresh работают как прежде

## Примеры использования

### В экране редактирования продукта
```javascript
import { useMainScreenRefresh } from '@entities/product';

const EditProductScreen = () => {
    const { triggerMainScreenRefresh } = useMainScreenRefresh();
    
    const handleSave = async () => {
        await saveProduct();
        triggerMainScreenRefresh(); // Обновляем главный экран
        navigation.goBack();
    };
};
```

### В экране управления товарами
```javascript
const ProductManagementScreen = () => {
    const { navigateToMainWithRefresh } = useMainScreenRefresh();
    
    const handleDeleteProduct = async () => {
        await deleteProduct();
        // Возвращаемся на главный экран с обновлением
        navigateToMainWithRefresh();
    };
};
```

Эта оптимизация значительно улучшает производительность приложения и пользовательский опыт, предотвращая ненужные перезагрузки данных при навигации.

