# Исправление проблемы с исчезновением заказов после обновления экрана

## Проблемы

### Проблема 1: Исчезновение заказов после pull-to-refresh

На экране заказов в обработке для администратора во вкладке "Активные" после обновления экрана (pull-to-refresh) заказы пропадали и не отображались даже после повторного обновления.

### Проблема 2: Исчезновение заказов после обработки заказа

После того как пользователь:
1. Заходит на детальный экран заказа
2. Обрабатывает заказ (завершает его)
3. Возвращается на экран списка заказов
4. Обновляет экран (pull-to-refresh)

Все заказы исчезают из списка.

### Проблема 3: История не показывает обработанные заказы ⚠️ СЕРВЕРНАЯ + КЛИЕНТСКАЯ

Во вкладке "История" для сотрудников (сборщика и курьера) после обработки заказов:
1. Переключаемся на вкладку "История"
2. Обработанные заказы не отображаются
3. Вместо них показываются старые данные с неправильными статусами

**Эта проблема имела ДВЕ части:**
- **Клиентская**: Race condition в переключении вкладок
- **Серверная**: После завершения этапа `assignedToId` очищался, поэтому сервер не находил заказы

### Причины

Все три проблемы имеют общий корень:

**Основные причины:**
1. Сотрудник завершает активные заказы (переводит их в статус `DELIVERED`)
2. При обновлении экрана сервер возвращает 0 активных заказов (все завершены)
3. В Redux state остаются старые данные с завершенными заказами
4. Мемоизация `stableStaffOrders` использовала зависимость только от `length`, что приводило к кэшированию старых данных
5. Фильтрация на клиенте получает старые завершенные заказы и отфильтровывает их все
6. Результат: пустой список, хотя в Redux были данные

**Дополнительные причины для Проблемы 3 (История):**

**7. Race condition (клиентская часть):**
   - Флаги `showHistory`, `showWaitingStock` изменялись **ДО** очистки и загрузки данных
   - `useOrderFiltering` пересчитывался с новыми флагами, но со старыми данными
   - Использовались `setTimeout` для задержки загрузки данных, что усугубляло проблему
   - Между изменением флага и загрузкой данных проходило время, в которое фильтрация работала некорректно

**8. Очистка assignedToId (серверная часть):**
   - После завершения этапа сервер очищал поле `assignedToId` в заказе
   - Фильтрация истории использовала условие `{ assignedToId: employeeId }` для поиска заказов сотрудника
   - Из-за очищенного `assignedToId` сервер НЕ находил только что обработанные заказы
   - Заказы "исчезали" из истории сотрудника сразу после обработки

### Логи проблемы

**Проблема 1 и 2 (pull-to-refresh):**
```
LOG  🔄 handleRefreshData: обновляем активные заказы
LOG  OrderApi: getOrders ответ получен {"dataLength": 0, ...}
LOG  ⚠️ useOrderFiltering: фильтрация не дала результатов {"initialCount": 20, "showHistory": false, "showWaitingStock": false}
```

Видно, что сервер вернул 0 заказов, но фильтрация пыталась обработать 20 старых заказов из Redux state.

**Проблема 3 (История):**
```
LOG  🔄 handleRefreshData: обновляем историю заказов
LOG  OrderApi: getOrders ответ получен {"dataLength": 0, ...}
LOG  🔍 useOrderFiltering: начало фильтрации {"staffOrdersLength": 20, "showHistory": true, "statuses": ["IN_DELIVERY", "PENDING", "DELIVERED", "CANCELLED", "PENDING"]}
```

Критично! Сервер вернул 0 заказов истории, но фильтрация получила 20 старых заказов с **неправильными статусами** (`IN_DELIVERY`, `PENDING`), которые затем отфильтровались как неподходящие для истории.

## Решение

Реализовано комплексное решение с исправлениями на **клиенте И сервере**:

## Клиентские исправления (Mobile)

### 1. Очистка данных перед обновлением

В функции `handleRefreshData` добавлена обязательная очистка данных перед загрузкой новых:

```javascript
const handleRefreshData = useCallback(async () => {
    setLocalRefreshing(true);
    try {
        console.log('🔄 handleRefreshData: начало обновления', { 
            showHistory, 
            showWaitingStock,
            currentOrdersCount: staffOrders?.length 
        });
        
        // Очищаем данные перед обновлением
        dispatch(clearStaffOrdersData());
        
        // Загружаем свежие данные
        if (showHistory) {
            await dispatch(fetchStaffOrders({ history: true, forceRefresh: true })).unwrap();
        } else if (showWaitingStock) {
            await dispatch(fetchStaffOrders({ status: 'WAITING_STOCK', forceRefresh: true })).unwrap();
        } else {
            await dispatch(fetchStaffOrders({ forceRefresh: true })).unwrap();
        }
        
        console.log('✅ handleRefreshData: обновление завершено');
    } catch (error) {
        console.error('❌ Ошибка при обновлении данных:', error);
    } finally {
        setLocalRefreshing(false);
    }
}, [showHistory, showWaitingStock, dispatch, staffOrders?.length]);
```

### 2. Исправление мемоизации

Изменена зависимость мемоизации для `stableStaffOrders` и `stableFilteredOrders`:

**Было:**
```javascript
const stableStaffOrders = useMemo(() => staffOrders || [], [staffOrders?.length]);
const stableFilteredOrders = useMemo(() => filteredOrders || [], [filteredOrders?.length]);
```

**Стало:**
```javascript
const stableStaffOrders = useMemo(() => {
    console.log('🔍 stableStaffOrders мемоизация обновлена:', { 
        length: staffOrders?.length,
        firstOrderId: staffOrders?.[0]?.id,
        firstOrderStatus: staffOrders?.[0]?.status
    });
    return staffOrders || [];
}, [staffOrders]); // Зависимость от самого массива, а не только от length

const stableFilteredOrders = useMemo(() => {
    console.log('🔍 stableFilteredOrders мемоизация обновлена:', { 
        length: filteredOrders?.length,
        firstOrderId: filteredOrders?.[0]?.id 
    });
    return filteredOrders || [];
}, [filteredOrders]); // Зависимость от самого массива
```

### 3. Исправление Race Condition в переключении вкладок

Переписаны функции `handleToggleHistory`, `handleToggleWaitingStock` и `handleToggleMain` для устранения race condition:

**Было (с race condition):**
```javascript
const handleToggleHistory = useCallback(() => {
    setShowHistory(true); // ← Флаг меняется сразу!
    setShowWaitingStock(false);
    dispatch(clearStaffOrdersData());
    
    setTimeout(() => { // ← Задержка создает race condition
        dispatch(fetchStaffOrders({ history: true, forceRefresh: true }));
    }, 50);
}, [dispatch]);
```

**Проблема:** `useOrderFiltering` пересчитывается с `showHistory=true`, но данные еще не загружены, поэтому работает со старыми данными.

**Стало (без race condition):**
```javascript
const handleToggleHistory = useCallback(async () => {
    try {
        // 1. Сначала очищаем данные СИНХРОННО
        dispatch(clearStaffOrdersData());
        
        // 2. Затем меняем флаги
        setShowHistory(true);
        setShowWaitingStock(false);
        
        // 3. И СРАЗУ загружаем новые данные (без setTimeout)
        await dispatch(fetchStaffOrders({ history: true, forceRefresh: true })).unwrap();
    } catch (error) {
        console.error('❌ Ошибка при переключении на историю:', error);
    }
}, [dispatch]);
```

Теперь:
- Данные очищаются синхронно перед изменением флагов
- Убраны `setTimeout` - загрузка происходит сразу
- Добавлена обработка ошибок
- `useOrderFiltering` работает только с актуальными данными

### 4. Расширенное логирование

Добавлено детальное логирование в `useOrderFiltering` для отслеживания процесса фильтрации:

```javascript
console.log('🔍 useOrderFiltering: начало фильтрации', {
    staffOrdersLength: staffOrders.length,
    showWaitingStock,
    showHistory,
    canViewAllOrders,
    actualProcessingRole,
    firstOrderStatus: staffOrders[0]?.status,
    firstOrderId: staffOrders[0]?.id,
    allStatuses: staffOrders.map(o => o.status),
    completedStatuses: staffOrders.filter(o => ['DELIVERED', 'CANCELLED', 'RETURNED'].includes(o.status)).length
});

// ... фильтрация ...

console.log('✅ useOrderFiltering: результат фильтрации', {
    initialCount: staffOrders.length,
    filteredCount: filtered.length,
    showHistory,
    showWaitingStock,
    canViewAllOrders,
    filteredStatuses: filtered.map(o => o.status)
});
```

## Серверные исправления (Backend)

### 5. Добавлены маркеры сотрудников в историю статусов

Во всех операциях с заказами добавляется маркер `[employee_id:X]` в комментарий истории:

**assignOrder** (взятие заказа):
```javascript
const historyComment = assignedToId 
    ? `Заказ назначен сотруднику ${newEmployee.name}... [employee_id:${assignedToId}]`
    : `Назначение с заказа снято...`;
```

**completeOrderStage** (завершение этапа):
```javascript
const finalComment = `${baseComment} [employee_id:${employeeId}]`;
```

**releaseOrder** (снятие с работы):
```javascript
const historyComment = `Заказ снят с работы... [employee_id:${employeeId}]`;
```

### 6. Обновлена фильтрация истории заказов

В `buildOrderWhereClause` добавлена проверка истории статусов:

```javascript
where.OR = [
    // Заказы, которые СЕЙЧАС назначены на сотрудника
    { assignedToId: parseInt(filters.employeeId) },
    
    // ✨ НОВОЕ: Заказы из истории обработки сотрудника
    {
        statusHistory: {
            some: {
                comment: {
                    contains: `employee_id:${filters.employeeId}`
                }
            }
        }
    },
    
    // Заказы его склада
    { warehouseId: parseInt(filters.employeeWarehouseId) }
];
```

Теперь заказы находятся даже после того, как `assignedToId` очищен!

### Улучшения UI (Mobile)

Также улучшено отображение пустого состояния для администраторов:

```javascript
const getMessage = useCallback(() => {
    if (showHistory) return 'История обработки пуста';
    if (showWaitingStock) return 'Нет заказов, ожидающих поставки товаров на склад';
    
    if (canViewAllOrders) {
        return 'Нет активных заказов для отображения.\nВсе заказы обработаны или находятся в истории.';
    }
    
    // ... остальная логика
}, [canViewAllOrders, showHistory, showWaitingStock, actualProcessingRole]);
```

Добавлена подсказка "Потяните вниз для обновления" для пустого состояния.

## Результат

После исправления:

### Технические улучшения:
1. ✅ При обновлении экрана старые завершенные заказы **немедленно удаляются** из Redux state перед загрузкой новых
2. ✅ Мемоизация теперь корректно обновляется при изменении содержимого массива, а не только длины
3. ✅ Новые данные с сервера корректно загружаются и немедленно отображаются
4. ✅ Детальное логирование позволяет отслеживать весь процесс обновления и фильтрации

### UX улучшения:
5. ✅ Если активных заказов нет, отображается понятное сообщение: *"Нет активных заказов для отображения. Все заказы обработаны или находятся в истории."*
6. ✅ Пользователь видит подсказку: *"Потяните вниз для обновления"*
7. ✅ Процесс обновления стал быстрее (убрана задержка 50мс)

### Исправленные сценарии:
- ✅ Pull-to-refresh на экране активных заказов когда все заказы завершены
- ✅ Возврат на экран списка после обработки заказа и обновление экрана
- ✅ Переключение между вкладками (Активные/История/Ожидают поставки) **без race condition**
- ✅ Обновление после массовой обработки заказов
- ✅ Отображение обработанных заказов во вкладке "История" для сотрудников
- ✅ Корректная фильтрация завершенных заказов в истории

## Дата исправления

29 октября 2025

## Связанные файлы

### Клиентская часть (Mobile):
- `mobile/src/screens/ordes/ui/StaffOrdersScreen/hooks/useStaffOrdersScreen.js` - основная логика обновления и мемоизация
- `mobile/src/screens/ordes/ui/StaffOrdersScreen/ui/StaffOrdersScreen.jsx` - UI компонент с улучшениями пустого состояния
- `mobile/src/entities/order/hooks/useOrderFiltering.js` - логика фильтрации с расширенным логированием
- `mobile/docs/STAFF_ORDERS_REFRESH_FIX.md` - документация клиентских исправлений

### Серверная часть (Backend):
- `server/src/services/order.service.js` - добавлены маркеры в историю и обновлена фильтрация
- `server/docs/ORDER_HISTORY_EMPLOYEE_FIX.md` - документация серверных исправлений

## Краткое резюме

**Проблемы:** 
1. Заказы исчезали из списка после обновления экрана (pull-to-refresh)
2. Заказы пропадали после обработки и возврата к списку
3. **История не показывала обработанные заказы** сотрудникам **← СЕРВЕРНАЯ + КЛИЕНТСКАЯ**

**Корень проблем:** 

**Клиентская часть:**
- Старые данные оставались в Redux state после обновления
- Мемоизация зависела только от длины массива, игнорируя изменения содержимого
- Фильтрация получала устаревшие данные и отфильтровывала все завершенные заказы
- **Race condition в переключении вкладок** - флаги менялись ДО загрузки данных

**Серверная часть (Проблема 3):**
- После завершения этапа поле `assignedToId` очищалось
- Фильтрация истории проверяла только текущий `assignedToId`, а не историю обработки
- Заказы "исчезали" из истории сразу после передачи следующему сотруднику

**Решение:**

**Клиентские исправления:**
1. **Очистка перед обновлением** - добавлен `clearStaffOrdersData()` перед `fetchStaffOrders()`
2. **Исправленная мемоизация** - зависимость от самого массива вместо только длины
3. **Устранение race condition** - синхронная очистка и загрузка данных без `setTimeout`
4. **Расширенное логирование** - детальное отслеживание процесса для отладки

**Серверные исправления (Проблема 3):**
1. **Маркеры в истории** - добавлен `[employee_id:X]` в комментарии всех операций с заказами
2. **Проверка statusHistory** - фильтрация истории теперь ищет маркеры в `statusHistory.comment`
3. **Полная история** - сотрудники видят все заказы, которые когда-либо обрабатывали

**Результат:** Заказы корректно обновляются и отображаются после любых операций:
- ✅ Pull-to-refresh работает правильно
- ✅ История показывает ВСЕ обработанные заказы (даже после передачи другим сотрудникам)
- ✅ Переключение вкладок без багов
- ✅ Все данные актуальны

