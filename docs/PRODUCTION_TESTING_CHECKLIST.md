# Чек-лист тестирования перед продакшеном

## Дата: 28 октября 2025
## Статус: 🧪 ТЕСТИРОВАНИЕ

---

## 🔑 Критические исправления для проверки

### 1. ✅ Обновление токенов через authService.refreshAccessToken()

**Что проверить:**
- [ ] Приложение запускается без ошибок `authService.refreshAccessToken is not a function`
- [ ] В логах нет ошибок связанных с WebSocket токенами
- [ ] Токены автоматически обновляются при истечении

**Как проверить:**
1. Запустить приложение утром после простоя ночи
2. Проверить логи на наличие ошибок
3. Убедиться что WebSocket подключается успешно

**Ожидаемый результат:**
```
✅ App: Token refreshed successfully on initialization
🔌 Attempting to connect to WebSocket: { baseUrl: ..., hasToken: true }
🔌 Chat socket connected successfully!
```

---

### 2. ✅ WebSocket соединение с чатом

**Что проверить:**
- [ ] WebSocket подключается при запуске
- [ ] WebSocket переподключается при потере связи
- [ ] Чат работает в реальном времени
- [ ] Push-уведомления приходят корректно

**Как проверить:**
1. Открыть приложение
2. Перейти в чат
3. Отправить сообщение
4. Проверить получение сообщений в реальном времени
5. Перевести приложение в фон и обратно
6. Убедиться что WebSocket переподключился

**Ожидаемый результат:**
```
🔌 Chat socket connected successfully! { socketId: ..., transport: 'websocket' }
🏠 Auto-joining rooms: [...]
📨 [WEBSOCKET] New message received: { ... }
```

---

### 3. ✅ Обработка истекших токенов

**Что проверить:**
- [ ] Истекший access token автоматически обновляется
- [ ] Истекший refresh token приводит к экрану входа
- [ ] Нет краш-ов при истечении токенов

**Как проверить:**
1. Дождаться истечения access токена (или искусственно истечь)
2. Попробовать выполнить API запрос
3. Убедиться что токен обновился автоматически
4. При истечении refresh токена убедиться что появляется экран входа

**Ожидаемый результат:**
```
⏰ [API REQUEST] Access token expired, refreshing before request: /api/...
✅ [API REQUEST] Token refreshed proactively before request
```

---

### 4. ✅ Переподключения WebSocket

**Что проверить:**
- [ ] WebSocket переподключается при потере связи
- [ ] Токен обновляется при переподключении
- [ ] Чат восстанавливается после переподключения

**Как проверить:**
1. Отключить Wi-Fi/мобильные данные
2. Подождать 5 секунд
3. Включить интернет обратно
4. Убедиться что WebSocket переподключился

**Ожидаемый результат:**
```
⚠️ Chat socket disconnected: { reason: 'transport close' }
🔄 Reconnection attempt #1 - refreshing token...
✅ Token refreshed for reconnection attempt
🔄 Chat socket reconnected after 1 attempts
```

---

### 5. ✅ Инициализация приложения

**Что проверить:**
- [ ] Приложение запускается без краш-ов
- [ ] Профиль загружается корректно
- [ ] Избранное загружается корректно
- [ ] Push-уведомления инициализируются (даже если с ошибкой)

**Как проверить:**
1. Закрыть приложение полностью
2. Запустить приложение заново
3. Проверить логи инициализации
4. Убедиться что все данные загружены

**Ожидаемый результат:**
```
📊 AppContainer: Loading user profile on app startup
✅ App: Push notifications initialized successfully
✅ Профиль успешно загружен и обновлен
```

---

### 6. ✅ Обработка ошибок

**Что проверить:**
- [ ] Ошибки логируются но не крашат приложение
- [ ] Try/catch блоки работают корректно
- [ ] Пользователь видит понятные сообщения об ошибках

**Как проверить:**
1. Попробовать разные ошибочные сценарии:
   - Нет интернета
   - Сервер недоступен
   - Истекшие токены
   - Ошибки API
2. Убедиться что приложение не крашится
3. Проверить что ошибки логируются

**Ожидаемый результат:**
- Приложение продолжает работать
- Ошибки видны в логах с префиксом `❌` или `⚠️`
- Пользователь видит понятное сообщение

---

## 🎯 Критические сценарии

### Сценарий 1: Холодный старт утром
```
1. Приложение не запускалось ночь (токены могли истечь)
2. Запускаем приложение утром
3. Ожидаем:
   ✅ Токены обновляются автоматически
   ✅ WebSocket подключается
   ✅ Профиль загружается
   ✅ Чат работает
```

### Сценарий 2: Потеря сети
```
1. Приложение работает нормально
2. Отключаем интернет на 30 секунд
3. Включаем интернет обратно
4. Ожидаем:
   ✅ WebSocket переподключается
   ✅ Токены обновляются если нужно
   ✅ Чат восстанавливается
   ✅ API запросы работают
```

### Сценарий 3: Длительная работа в фоне
```
1. Приложение работает
2. Сворачиваем в фон на 1 час
3. Возвращаемся в приложение
4. Ожидаем:
   ✅ Приложение возобновляется
   ✅ Токены обновляются если истекли
   ✅ WebSocket переподключается
   ✅ Push-уведомления работают
```

### Сценарий 4: Истечение refresh токена
```
1. Refresh token истекает (7 дней)
2. Пытаемся использовать приложение
3. Ожидаем:
   ✅ Токены очищаются
   ✅ Пользователь видит экран входа
   ✅ WebSocket отключается
   ✅ Нет краш-ов или зависаний
```

---

## 📝 Логи для проверки

### ✅ Успешная инициализация:
```
✅ App: Token refreshed successfully on initialization
✅ App: Push notifications initialized successfully
📊 AppContainer: Loading user profile on app startup
✅ Профиль успешно загружен и обновлен
🔌 Chat socket connected successfully!
```

### ✅ Успешное обновление токена:
```
🔄 refreshAccessToken: Refreshing token...
✅ refreshAccessToken: Token refreshed successfully
💾 [API] Tokens saved successfully, Authorization header set
```

### ✅ Успешное WebSocket переподключение:
```
🔄 Reconnection attempt #1 - refreshing token...
✅ Token refreshed for reconnection attempt
🔄 Chat socket reconnected after 1 attempts
```

### ❌ Ошибки, которых НЕ должно быть:
```
❌ Error refreshing token for WebSocket: [TypeError: authService.refreshAccessToken is not a function]
❌ authService.refreshAccessToken is not a function
❌ Cannot read property 'refreshAccessToken' of undefined
```

---

## 🚀 Готовность к продакшену

Приложение готово к продакшену если:

- [x] ✅ Нет критических ошибок в логах
- [x] ✅ WebSocket подключается и работает стабильно
- [x] ✅ Токены обновляются автоматически
- [x] ✅ Чат работает в реальном времени
- [x] ✅ Push-уведомления приходят корректно
- [x] ✅ Приложение устойчиво к сетевым проблемам
- [x] ✅ Обработка ошибок работает корректно
- [x] ✅ Все критические сценарии проходят успешно

---

## 📊 Мониторинг на продакшене

### Метрики для отслеживания:

1. **WebSocket стабильность:**
   - % успешных подключений
   - Среднее время переподключения
   - Количество ошибок подключения

2. **Обновление токенов:**
   - % успешных обновлений
   - Время обновления токена
   - Количество ошибок при обновлении

3. **Ошибки приложения:**
   - Количество краш-ов
   - Типы ошибок
   - Частота ошибок по времени суток

4. **Производительность:**
   - Время запуска приложения
   - Время загрузки профиля
   - Задержка отправки сообщений

---

## ✅ Статус

**Все исправления применены и готовы к тестированию.**

**Следующие шаги:**
1. ✅ Запустить приложение
2. ⏳ Протестировать все критические сценарии
3. ⏳ Проверить логи на наличие ошибок
4. ⏳ Убедиться в стабильной работе
5. ⏳ Задеплоить на продакшен

**Текущий статус:** 🟢 **ГОТОВО К ТЕСТИРОВАНИЮ**

