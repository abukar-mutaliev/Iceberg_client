# Исправление Push-уведомлений OneSignal

## Проблема
Push-уведомления через OneSignal не приходили из-за того, что **Player ID (Subscription ID) не получался** после инициализации.

### Симптомы из логов:
```
[WARNING] 🎫 OneSignal Player ID: нет
[ERROR] ❌ Не удалось настроить OneSignal для пользователя
[ERROR] ❌ Player ID не получен
```

## Причины проблемы

1. **Отсутствие ожидания регистрации устройства** - OneSignal требует время (2-3 секунды) после инициализации для регистрации устройства в Firebase Cloud Messaging (FCM)

2. **Недостаточное логирование** - было сложно определить на каком этапе происходит сбой

3. **Отсутствие повторных попыток** - метод `getSubscriptionId()` делал только одну попытку получить Player ID

4. **Синхронная проверка разрешений** - не было явной проверки что разрешения на уведомления действительно получены

## Внесенные изменения

### 1. OneSignalService.js

#### Метод `initialize()`
- ✅ Добавлено подробное логирование всех этапов инициализации
- ✅ Добавлена проверка разрешений с логированием результата
- ✅ Улучшена обработка ошибок

#### Метод `getSubscriptionId()` - **КРИТИЧЕСКОЕ ИЗМЕНЕНИЕ**
```javascript
// Было:
async getSubscriptionId() {
    const deviceState = await oneSignal.User.pushSubscription.getIdAsync();
    return deviceState;
}

// Стало:
async getSubscriptionId(retries = 5, delayMs = 2000) {
    for (let attempt = 1; attempt <= retries; attempt++) {
        const subscriptionId = await oneSignal.User.pushSubscription.getIdAsync();
        if (subscriptionId) {
            return subscriptionId;
        }
        if (attempt < retries) {
            await new Promise(resolve => setTimeout(resolve, delayMs));
        }
    }
    return null;
}
```

**Почему это важно:**
- OneSignal регистрирует устройство в FCM асинхронно
- Первая попытка получить Player ID может вернуть `null`
- Повторные попытки с задержкой дают время на регистрацию

#### Метод `initializeForUser()` - **КРИТИЧЕСКОЕ ИЗМЕНЕНИЕ**
```javascript
// Добавлено:
// 1. Подробное логирование каждого шага
console.log('[OneSignal] ===== Начинаем инициализацию для пользователя =====');

// 2. Ожидание регистрации устройства (ВАЖНО!)
await new Promise(resolve => setTimeout(resolve, 3000));

// 3. Использование улучшенного getSubscriptionId() с повторами
const subscriptionId = await this.getSubscriptionId();
```

#### Новые методы для диагностики:
- `getPushToken()` - получение FCM токена
- `getOptedIn()` - проверка статуса подписки

#### Улучшенное логирование:
- `setExternalUserId()` - логи установки User ID
- `saveSubscriptionToServer()` - логи сохранения токена
- `clearUserContext()` - логи очистки контекста
- `setUserTags()` - логи установки тегов

### 2. PushNotificationService.js

#### Методы `initialize()` и `initializeForUser()`
- ✅ Добавлено подробное логирование
- ✅ Добавлена проверка входных данных
- ✅ Улучшена обработка ошибок

#### Метод `getOneSignalRuntimeInfo()`
- ✅ Добавлено логирование каждого шага диагностики
- ✅ Логи помогают понять где именно возникают проблемы

## Как тестировать

### Вариант 1: Через диагностический экран

1. **Откройте приложение** и перейдите в:
   - Профиль → Настройки → Диагностика Push-уведомлений

2. **Выполните последовательно:**
   ```
   Шаг 1: 🧹 Очистить (очистка старого состояния)
   Шаг 2: 🚀 Инициализация
   Шаг 3: 👤 Для пользователя
   Шаг 4: 🎫 Player ID (должен появиться!)
   ```

3. **Проверьте логи** - они теперь подробные:
   ```
   [OneSignal] ===== Начинаем инициализацию для пользователя =====
   [OneSignal] SDK инициализирован
   [OneSignal] Ожидаем регистрацию устройства (3 секунды)...
   [OneSignal] Попытка 1/5 получить Player ID...
   [OneSignal] ✅ Player ID успешно получен: 3d4ff19a...
   ```

4. **Отправьте тестовое уведомление:**
   - Нажмите кнопку "📨 Тест Push"
   - Уведомление должно прийти!

### Вариант 2: При входе в приложение

1. **Выйдите из приложения** (если были залогинены)

2. **Войдите заново** - теперь в логах будет видно:
   ```
   [PushService] ===== Инициализация для пользователя =====
   [OneSignal] Устанавливаем External User ID: 13
   [OneSignal] Ожидаем регистрацию устройства (3 секунды)...
   [OneSignal] ✅ Player ID получен
   [OneSignal] ✅ Токен сохранен на сервер
   ```

3. **Проверьте токены на сервере:**
   - В диагностике нажмите "📋 Токены сервера"
   - Должен появиться новый активный OneSignal токен

### Вариант 3: Реальное уведомление

1. **Убедитесь что Player ID получен** (через диагностику)

2. **Отправьте сообщение в чат** с другого аккаунта

3. **Уведомление должно прийти!**

## Ожидаемые результаты

### До исправления:
```
[WARNING] 🎫 OneSignal Player ID: нет
[ERROR] ❌ Не удалось настроить OneSignal для пользователя
```

### После исправления:
```
[SUCCESS] 🎫 OneSignal Player ID: 3d4ff19a-46c7-4df1-b824-2b51f9956a64
[SUCCESS] ✅ Токен сохранен на сервер
[SUCCESS] 📨 Тестовое уведомление отправлено
```

## Технические детали

### Почему нужна задержка 3 секунды?

OneSignal на Android использует Firebase Cloud Messaging (FCM). Процесс регистрации:

1. `OneSignal.initialize()` вызывается
2. OneSignal SDK регистрирует приложение в FCM (асинхронно)
3. FCM возвращает токен
4. OneSignal создает Player ID на своих серверах
5. Player ID становится доступен через `getIdAsync()`

Этот процесс занимает 2-4 секунды. Без ожидания `getIdAsync()` возвращает `null`.

### Повторные попытки

Метод `getSubscriptionId()` делает до 5 попыток с интервалом 2 секунды:
- **Попытка 1:** сразу после login (может быть null)
- **Попытка 2:** через 2 секунды (обычно уже есть)
- **Попытки 3-5:** резервные на случай медленного соединения

### FCM токен vs Player ID

- **FCM Token** - это токен Firebase для доставки уведомлений
- **Player ID** (Subscription ID) - это ID OneSignal для управления подписками
- OneSignal использует FCM под капотом, но нам нужен Player ID для API

## Проверка работоспособности

### Минимальный чеклист:
- ✅ Player ID получается после инициализации
- ✅ Токен сохраняется на сервер
- ✅ Тестовое уведомление приходит
- ✅ Реальные уведомления приходят

### Логи для проверки:
```bash
# Фильтр логов OneSignal
adb logcat | grep "\[OneSignal\]"

# Фильтр логов PushService
adb logcat | grep "\[PushService\]"

# Все логи push-уведомлений
adb logcat | grep -E "\[OneSignal\]|\[PushService\]"
```

## Дополнительные улучшения

### Если проблемы продолжаются:

1. **Проверьте google-services.json:**
   ```bash
   # Должен быть актуальный файл из Firebase Console
   cat mobile/google-services.json
   ```

2. **Проверьте OneSignal App ID:**
   ```bash
   # В app.config.js должно быть:
   oneSignalAppId: 'a1bde379-4211-4fb9-89e2-3e94530a7041'
   ```

3. **Проверьте разрешения в AndroidManifest.xml:**
   ```xml
   <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
   ```

4. **Пересоберите приложение:**
   ```bash
   cd mobile
   eas build --profile preview --platform android
   ```

## Заключение

Основная проблема была в **отсутствии ожидания** регистрации устройства в FCM. OneSignal нужно время для асинхронной регистрации, и метод `getIdAsync()` возвращал `null` при немедленном вызове.

**Решение:** Добавлена задержка 3 секунды + повторные попытки с интервалом 2 секунды.

Теперь push-уведомления должны работать стабильно! 🎉

