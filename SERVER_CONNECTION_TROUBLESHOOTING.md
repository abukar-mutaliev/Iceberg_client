# Устранение проблем подключения к серверу

## Проблема: ERR_NETWORK при подключении к серверу

### Диагностика

1. **Проверка доступности сервера с компьютера:**
   ```powershell
   Test-NetConnection -ComputerName 85.192.33.223 -Port 5000
   ```
   
   Если `TcpTestSucceeded: False` - сервер недоступен.

2. **Проверка доступности сервера через curl:**
   ```bash
   curl http://85.192.33.223:5000/api/health
   ```

### Решения

#### 1. Проверьте, что сервер запущен на сервере:
```bash
# На сервере проверьте, что процесс слушает порт 5000
netstat -tuln | grep 5000
# или
ss -tuln | grep 5000
```

#### 2. Проверьте файрвол на сервере:
```bash
# Для Ubuntu/Debian
sudo ufw status
sudo ufw allow 5000/tcp

# Для CentOS/RHEL
sudo firewall-cmd --list-ports
sudo firewall-cmd --permanent --add-port=5000/tcp
sudo firewall-cmd --reload
```

#### 3. Убедитесь, что сервер слушает на всех интерфейсах:
Сервер должен слушать на `0.0.0.0:5000`, а не на `127.0.0.1:5000` или `localhost:5000`.

#### 4. Для Expo Go - создайте development build:

**Важно:** В Expo Go `network_security_config.xml` НЕ применяется!

Для применения настроек сети нужно создать development build:

```bash
# 1. Создайте нативные файлы
npx expo prebuild --clean

# 2. Запустите development build
npx expo run:android

# Или создайте через EAS
eas build --profile development --platform android
```

#### 5. Проверьте настройки в app.config.js:
- `usesCleartextTraffic: true` - должно быть включено
- `networkSecurityConfig: './network_security_config.xml'` - должно быть указано

### Текущая конфигурация

- **API URL:** `http://85.192.33.223:5000` (из app.config.js)
- **Network Security Config:** `./network_security_config.xml`
- **Cleartext Traffic:** разрешен для всех доменов

### Логи для диагностики

В приложении включено детальное логирование:
- `🌐 [API] Using API URL from config:` - показывает используемый URL
- `🌐 [API ERROR] Network connection error:` - детальная информация об ошибке

### Следующие шаги

1. Убедитесь, что сервер доступен с вашего компьютера
2. Проверьте файрвол на сервере
3. Создайте development build (не используйте Expo Go для тестирования сетевых настроек)
4. Проверьте логи приложения для детальной диагностики

