# Инструкция по публикации приложения в Play Market и OTA обновлениям

## 📱 Публикация в Google Play Market

### Шаг 1: Подготовка к первой публикации

1. **Создайте аккаунт разработчика Google Play**
   - Перейдите на https://play.google.com/console
   - Зарегистрируйтесь (одноразовый платеж $25)

2. **Создайте приложение в Google Play Console**
   - Нажмите "Создать приложение"
   - Заполните основную информацию (название, описание, категория и т.д.)

3. **Подготовьте материалы для публикации:**
   - Иконка приложения (512x512 px)
   - Скриншоты (минимум 2, рекомендуется 8)
   - Графический баннер (1024x500 px)
   - Краткое описание (до 80 символов)
   - Полное описание (до 4000 символов)

### Шаг 2: Сборка production версии

```bash
# Сборка Android App Bundle (AAB) для Play Market
npm run build:production

# Или напрямую через EAS CLI
eas build --profile production --platform android
```

**Важно:** 
- Сборка займет 15-30 минут
- После завершения вы получите ссылку на скачивание AAB файла
- Сохраните этот файл - он понадобится для загрузки в Play Console

### Шаг 3: Загрузка в Google Play Console

1. **Войдите в Google Play Console**
2. **Перейдите в раздел "Производство" (Production)**
3. **Создайте новый релиз:**
   - Нажмите "Создать релиз"
   - Загрузите AAB файл, который вы получили после сборки
   - Заполните "Что нового в этой версии" (release notes)
4. **Заполните контент-рейтинг и другие обязательные разделы**
5. **Отправьте на проверку**

### Шаг 4: Настройка подписи приложения (если еще не настроено)

Если это первая публикация, EAS автоматически создаст ключ подписи. Сохраните его в безопасном месте!

Для последующих обновлений используйте тот же ключ.

---

## 🔄 OTA (Over-The-Air) Обновления

После публикации приложения в Play Market, вы можете обновлять JavaScript код, ресурсы и некоторые нативные изменения **без пересборки и повторной публикации в Play Market**.

### Как работают OTA обновления

1. **Пользователи получают обновления автоматически** при запуске приложения
2. **Обновления применяются в фоне** - пользователь не заметит процесса
3. **Работает только для JavaScript кода** - изменения нативного кода требуют новой сборки

### Публикация обновления

```bash
# Для production окружения
npm run update:production

# Или напрямую
eas update --branch production --message "Описание изменений"
```

### Что можно обновлять через OTA:

✅ JavaScript/TypeScript код  
✅ React компоненты  
✅ Стили и UI  
✅ Изображения и другие ресурсы  
✅ Конфигурация (если не требует нативных изменений)  
✅ Логика приложения  

### Что НЕЛЬЗЯ обновлять через OTA:

❌ Нативные модули (добавление/удаление)  
❌ Изменения в `app.config.js` (требуют пересборки)  
❌ Изменения в `eas.json`  
❌ Версия приложения (`version` и `versionCode`)  
❌ Новые разрешения (permissions)  
❌ Изменения в нативных плагинах  

### Важные моменты:

1. **Runtime Version**
   - Текущий `runtimeVersion: '1.0.0'` в `app.config.js`
   - При изменении нативного кода нужно изменить `runtimeVersion` (например, на `1.0.1`)
   - Обновления работают только в рамках одного `runtimeVersion`

2. **Каналы обновлений (Channels)**
   - Production использует канал `production`
   - Preview использует канал `preview`
   - Каждый канал получает свои обновления независимо

3. **Проверка обновлений**
   - Приложение автоматически проверяет обновления при запуске
   - Настройка: `updates.checkAutomatically: 'ON_LOAD'` (по умолчанию)

---

## 📋 Типичный workflow разработки

### Разработка новой функции:

1. **Разработка в dev режиме:**
   ```bash
   npm start
   ```

2. **Тестирование в preview сборке:**
   ```bash
   npm run build  # создает preview сборку
   ```

3. **Публикация preview обновления (если нужно):**
   ```bash
   npm run update:preview
   ```

### Публикация в production:

1. **Если изменения только в JS коде:**
   ```bash
   npm run update:production
   ```
   - Обновление будет доступно пользователям в течение нескольких минут

2. **Если нужны изменения в нативном коде:**
   ```bash
   # Обновите versionCode в app.config.js
   # Затем соберите новую версию
   npm run build:production
   # Загрузите AAB в Play Console
   ```

---

## 🔧 Полезные команды

```bash
# Сборка production версии для Android
npm run build:production

# Сборка production версии для iOS
npm run build:production:ios

# Публикация OTA обновления для production
npm run update:production

# Публикация OTA обновления для preview
npm run update:preview

# Просмотр статуса сборки
eas build:list

# Просмотр истории обновлений
eas update:list
```

---

## ⚠️ Важные замечания

1. **Версионирование:**
   - `version` в `app.config.js` - версия для пользователей (например, "1.0.7")
   - `versionCode` в `app.config.js` - внутренний номер версии для Android (должен увеличиваться с каждой публикацией в Play Market)
   - `runtimeVersion` - версия нативного кода (меняется только при изменении нативного кода)

2. **Безопасность:**
   - Никогда не публикуйте обновления с критическими багами
   - Тестируйте обновления на preview канале перед публикацией в production
   - Используйте откат (rollback) если что-то пошло не так

3. **Мониторинг:**
   - Следите за ошибками через Expo Dashboard
   - Проверяйте метрики обновлений
   - Используйте аналитику для отслеживания успешности обновлений

---

## 🆘 Решение проблем

### Обновления не применяются:
- Проверьте, что `updates.enabled: true` в `app.config.js`
- Убедитесь, что `runtimeVersion` совпадает
- Проверьте интернет-соединение на устройстве

### Ошибки при сборке:
- Проверьте логи в EAS Dashboard
- Убедитесь, что все зависимости установлены
- Попробуйте очистить кэш: `eas build --clear-cache`

### Проблемы с подписью:
- Убедитесь, что используете правильный ключ
- Проверьте настройки в Google Play Console → Настройка приложения → Подпись приложения

---

## 📚 Дополнительные ресурсы

- [EAS Build Documentation](https://docs.expo.dev/build/introduction/)
- [EAS Update Documentation](https://docs.expo.dev/eas-update/introduction/)
- [Google Play Console Help](https://support.google.com/googleplay/android-developer)



