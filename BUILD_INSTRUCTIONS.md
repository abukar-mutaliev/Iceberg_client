# 🔨 Инструкция по пересборке с правильным OneSignal App ID

## Проблема
В конфиге правильный App ID: `a1bde379-4211-4fb9-89e2-3e94530a7041`
Но в runtime используется: `4ad5c8d5-4818-4e13-beac-fb1ec63285ad`

Это значит что **нативный код** скомпилирован со старым App ID.

## Решение: Пересборка через EAS Build

### 1. Убедитесь что App ID правильный в `app.config.js`:

```javascript
extra: {
    oneSignalAppId: 'a1bde379-4211-4fb9-89e2-3e94530a7041',
}
```

✅ УЖЕ ПРАВИЛЬНО в вашем файле!

### 2. Пересоберите preview сборку:

```bash
cd mobile
eas build --profile preview --platform android
```

Это займет ~10-15 минут.

### 3. Установите новую сборку на устройство

После завершения сборки:
1. Скачайте APK из ссылки которую покажет EAS
2. Установите на устройство
3. Очистите данные: `adb shell pm clear com.abuingush.iceberg`
4. Запустите приложение

### 4. Проверьте что App ID правильный:

```powershell
# Смотрите логи при запуске
adb logcat -v time | Select-String -Pattern "OneSignal"

# Должны увидеть:
# "appId":"a1bde379-4211-4fb9-89e2-3e94530a7041"
```

---

## Альтернатива: Локальная сборка (быстрее для теста)

Если нужен быстрый тест:

```bash
cd mobile

# Development client сборка
npx expo run:android

# Или preview сборка локально
npx eas build --profile preview --platform android --local
```

---

## Проверка после пересборки

1. **Запустите приложение**
2. **Войдите под любым пользователем** (кроме ID: 13)
3. **Смотрите логи:**

```powershell
adb logcat -d -v time | Select-String -Pattern "OneSignal" | Select-Object -Last 20
```

### ✅ Ожидаемые логи:
```
I/OneSignal: appId: a1bde379-4211-4fb9-89e2-3e94530a7041
I/OneSignal: Got Response = POST - STATUS: 200
I/OneSignal: Player ID: НОВЫЙ_PLAYER_ID
```

### ❌ Если все еще используется старый App ID:
```
I/OneSignal: appId: 4ad5c8d5-4818-4e13-beac-fb1ec63285ad
```

Значит проблема в кэшировании build. Попробуйте:
```bash
cd mobile
rm -rf android/build
rm -rf android/app/build
npx expo prebuild --clean
npx expo run:android
```

---

## ⚡ Быстрое решение БЕЗ пересборки

Если не хотите ждать пересборку, используйте **текущий App ID** как основной:

1. Откройте OneSignal Dashboard: https://app.onesignal.com/
2. Найдите приложение с ID: `4ad5c8d5-4818-4e13-beac-fb1ec63285ad`
3. Используйте ЕГО для уведомлений

Но это не рекомендуется, так как в коде используется другой App ID.

---

## 📊 Текущее состояние

| Параметр | Значение | Статус |
|----------|----------|---------|
| App ID в app.config.js | `a1bde379...` | ✅ Правильно |
| App ID в runtime | `4ad5c8d5...` | ❌ Неправильно |
| Player ID создается | Да (`28bbb5fe...`) | ✅ Работает |
| User ID 13 | Заблокирован | ❌ Проблема |
| User ID 17 | Работает | ✅ OK |
| Разрешения | Есть | ✅ OK |
| Лимит тегов | Превышен | ⚠️ Не критично |

---

## 🎯 Рекомендация

**ВАРИАНТ А (правильный):**
1. Пересоберите приложение через EAS Build
2. Установите новую сборку
3. Проверьте что используется правильный App ID

**ВАРИАНТ Б (быстрый):**
1. Продолжайте использовать текущее приложение
2. Настройте уведомления через App ID: `4ad5c8d5-4818-4e13-beac-fb1ec63285ad`
3. Пересоберите потом когда будет время

---

## 💡 Почему Player ID создается с неправильным App ID?

OneSignal плагин (`onesignal-expo-plugin`) устанавливает App ID **во время prebuild/build**.
Это значит что он "зашит" в нативный код и не меняется без пересборки.

Поэтому:
- ✅ Все мои исправления с задержками и повторами **РАБОТАЮТ**
- ✅ Player ID **СОЗДАЕТСЯ УСПЕШНО**
- ❌ Но используется **СТАРЫЙ App ID** из предыдущей сборки

